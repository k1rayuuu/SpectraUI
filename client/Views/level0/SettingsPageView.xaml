<UserControl x:Class="SpectraUI.Views.level0.SettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpectraUI.Views.level0"
             mc:Ignorable="d" 
             d:DesignHeight="568" d:DesignWidth="850">
    <Grid>
        <Border Background="#0c0c0c" CornerRadius="8">
            <Grid>
                <RadioButton HorizontalAlignment="Left" VerticalAlignment="Bottom" Command="{Binding NavigateSettingsPageCommand}" Style="{StaticResource Btn_Style}" Cursor="Hand" Margin="24">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Назад" Style="{StaticResource Text_Style}" Margin="0"/>
                    </StackPanel>
                </RadioButton>
                <TextBlock Text="Настройки" Margin="0" HorizontalAlignment="Center" Style="{StaticResource Text_Style}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
