<UserControl x:Class="SpectraUI.Views.level0.AuthPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SpectraUI.Views.level0"
             mc:Ignorable="d" 
             d:DesignHeight="568" d:DesignWidth="850">
    <Grid>
        <Border Background="#0c0c0c" CornerRadius="8">
            <Grid>
                <Border Margin="46" Width="296" Height="360" Background="#191919" CornerRadius="8">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="20">
                        <Image Source="/Images/SpectraUILogo.png" VerticalAlignment="Center" Height="40" Width="40"/>
                        <TextBlock Text="SpectraUI" Style="{StaticResource Text_Style}" FontFamily="Nunito Bold" FontSize="26"/>
                        </StackPanel>
                        <TextBox Style="{StaticResource SpectraTextBox}" Margin="8"/>
                        <TextBox Style="{StaticResource SpectraTextBox}" Margin="8"/>
                        <RadioButton Grid.Row="5" Style="{StaticResource Btn1_Style}" Cursor="Hand" Margin="32">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="Войти" Style="{StaticResource Text_Style}" Margin="0"/>
                            </StackPanel>
                        </RadioButton>
                        
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
